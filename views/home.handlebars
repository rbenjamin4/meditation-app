<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<link rel="stylesheet" href="/css/progress-bar.css">

<header class="marbled-bg">

    <div class="weekly-duration">
        <div role="progress-bar" aria-valuenow="{{user.dataValues.timeListened}}" aria-valuemin="0" aria-valuemax="{{user.dataValues.weeklyGoal}}" style="--value: {{user.dataValues.timeListened}};"></div>
        <p style="font-size: 60px; font-weight: 800; text-align: center;"></p>
        <h1>Weekly Practice Duration</h1>
    </div>
    <button type="button" id="menu" onclick="location.href='/api/meditations/'">
        <img src="/images/menu-list.svg">
    </button>

</header>

<section class="recent-meditations">
    <p>Recently Played</p>
    {{#each data}}
    <article class="meditation">
        <div class="meditation-info">
            <h2>{{dataValues.title}}</h2>
            <p>{{instructor.dataValues.name}}</p>
            <p>{{dataValues.duration}}m</p>
        </div>
        <button type="button" class="play-button" onclick="location.href='/api/meditations/{{dataValues.id}}'">
            <i class="fa fa-play-circle fa-4x"></i>
        </button>
    </article>
    {{/each}}
</section>

<style>
    body {
        background: unset;
    }
</style>